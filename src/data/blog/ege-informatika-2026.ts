import type { BlogPost } from "@/data/blog/posts";

const COVER = "/landing/icon_sigma.png";
const PUBLISHED = "2026-09-11";

function py(code: string): string {
  return `\n\`\`\`python\n${code.trim()}\n\`\`\`\n`;
}

function task(args: {
  n: number;
  label: string;
  description: string;
  keywords: string[];
  readingTime: number;
  body: string;
}): BlogPost {
  return {
    slug: `ege-informatika-2026-zadanie-${args.n}`,
    title: `ЕГЭ по информатике 2026, задание ${args.n}: ${args.label}`,
    h1: `ЕГЭ по информатике 2026, задание ${args.n}: ${args.label}`,
    description: args.description,
    keywords: [
      "ЕГЭ информатика 2026",
      `задание ${args.n} ЕГЭ информатика`,
      args.label,
      ...args.keywords,
    ],
    publishedAt: PUBLISHED,
    updatedAt: PUBLISHED,
    readingTime: args.readingTime,
    coverImage: COVER,
    coverAlt: `Конспект к заданию ${args.n} ЕГЭ по информатике 2026`,
    series: "ege-informatika-2026",
    seriesOrder: args.n,
    seriesLabel: args.label,
    body: args.body.trim(),
  };
}

export const EGE_INFORMATIKA_2026_SERIES = "ege-informatika-2026";

export const egeInformatika2026Posts: BlogPost[] = [
  task({
    n: 1,
    label: "графы и кратчайший путь",
    readingTime: 7,
    description:
      "Как решать задание 1 ЕГЭ по информатике 2026: таблица дорог, схема и кратчайший путь. Конспект и шаблон поиска пути на Python.",
    keywords: ["кратчайший путь", "графы ЕГЭ", "информационные модели"],
    body: `
Задание 1 проверяет умение читать информационную модель: схему дорог, карту, таблицу расстояний или формулу. В 2026 году линия не менялась. Это базовое задание на 1 балл, файл к нему не выдают, обычно хватает 3 минут.

## Как выглядит условие

Чаще всего дают пункты и дороги между ними. Нужно найти длину кратчайшего пути между двумя пунктами или число пунктов на таком пути. Дороги бывают двусторонними и односторонними, с весами и без. Иногда вместо схемы — таблица, где пустая клетка или прочерк означает «дороги нет».

## Конспект

1. Перенесите данные на черновик. Кружки — пункты, линии — дороги. Подпишите веса.
2. Если таблица, проверьте, симметрична ли она. Если нет — дороги односторонние.
3. Идите от старта. Для маленького графа достаточно перебрать маршруты без циклов. Цикл только удлиняет путь, его можно не рассматривать.
4. Записывайте не «красивый» путь, а минимальную уже найденную длину. Когда появляется более короткий, заменяйте.
5. Перечитайте вопрос: спрашивают длину, число дорог или число промежуточных пунктов? Это разные ответы.
6. Если путь должен проходить через заданный пункт, решайте две отдельные задачи: старт → обязательный пункт и он → финиш. Сумма кратчайших и есть ответ, если других ограничений нет.

## Шаблон на Python

На экзамене граф маленький, и ручной перебор часто быстрее. Программа полезна, чтобы не сбиться в таблице с весами. Ниже — Дейкстра для неориентированного взвешенного графа. Рёбра задаются списком \`A B вес\`.

${py(`
import heapq

edges = [
    ("A", "B", 4),
    ("A", "C", 2),
    ("B", "D", 5),
    ("C", "D", 1),
]
start, goal = "A", "D"

graph = {}
for a, b, w in edges:
    graph.setdefault(a, []).append((b, w))
    graph.setdefault(b, []).append((a, w))  # уберите строку, если дороги односторонние

dist = {start: 0}
heap = [(0, start)]
while heap:
    cur, node = heapq.heappop(heap)
    if cur != dist.get(node):
        continue
    if node == goal:
        break
    for nxt, w in graph.get(node, []):
        nd = cur + w
        if nd < dist.get(nxt, 10**9):
            dist[nxt] = nd
            heapq.heappush(heap, (nd, nxt))

print(dist.get(goal))
`)}

Если веса все равны 1, тот же ответ даст обход в ширину: длина пути — число рёбер.

## Типичные ошибки

- Пропущено направление стрелки на схеме.
- В ответ записано число вершин, хотя спрашивали длину.
- Посчитан путь «через все пункты», хотя такого условия нет.
- Для обязательной вершины взята сумма не кратчайших кусков.

Сверьте готовый ответ с одним явным маршрутом на черновике. Если программа выдала число, которого нет ни у одного пути, где-то лишнее ребро.
`,
  }),
  task({
    n: 2,
    label: "таблицы истинности",
    readingTime: 8,
    description:
      "Задание 2 ЕГЭ по информатике 2026: как восстановить переменные по фрагменту таблицы истинности. Конспект и перебор выражений на Python.",
    keywords: ["таблица истинности", "логическая схема", "импликация"],
    body: `
Задание 2 — базовое, 1 балл. Нужно сопоставить логические выражения столбцам таблицы истинности и записать порядок переменных, например \`xywz\`. Файла нет.

ФИПИ отдельно предупреждает: заполненная таблица не должна содержать одинаковых строк. Если при подстановке получились две одинаковые строки, переменные расставлены неверно, даже если «почти всё сошлось».

## Конспект

Операции, которые встречаются постоянно:

- конъюнкция \`A ∧ B\` — истина только когда оба истинны;
- дизъюнкция \`A ∨ B\` — ложна только когда оба ложны;
- отрицание \`¬A\`;
- импликация \`A → B\` ложна только на наборе «A истина, B ложь». В программе это \`not A or B\`;
- эквиваленция \`A ≡ B\` истинна, когда значения равны.

Порядок работы:

1. Выпишите, сколько переменных. Четыре переменные — 16 наборов, в фрагменте обычно 3–5 строк.
2. Найдите столбец с самым узнаваемым узором. Например, функция, истинная ровно на одном наборе, часто содержит конъюнкцию нескольких литералов.
3. Поставьте гипотезу о соответствии столбцов выражениям. Перебирайте перестановки переменных, а не подбирайте «на глаз» с середины.
4. Проверьте каждую данную строку. Ошибка в одной клетке — гипотеза неверна.
5. В конце убедитесь, что в полной таблице нет совпадающих строк, если условие это требует.

Ответ пишут без пробелов и знаков препинания, буквами именно в том порядке, который указан в задании.

## Шаблон на Python

Функция ниже перебирает, какая переменная в каком столбце. Замените \`exprs\` на выражения из условия. \`row\` — известные значения функции по столбцам.

${py(`
from itertools import permutations, product

vars_ = ["x", "y", "z", "w"]

def F1(x, y, z, w):
    return int((not x or y) and (not y or z))

def F2(x, y, z, w):
    return int(x == w)

# фрагмент: списки значений переменных неизвестны,
# известны только столбцы F. Подставьте свои функции и строки таблицы.
known = [
    (0, 1),
    (1, 0),
    (1, 1),
]

# полный перебор наборов, если нужно построить таблицу с нуля
for values in product([0, 1], repeat=4):
    env = dict(zip(vars_, values))
    print(values, F1(**env), F2(**env))

print("--- перестановки для сопоставления столбцов ---")
for order in permutations(vars_):
    ok = True
    # сюда подставьте конкретные наборы из таблицы, когда восстановите биты
    print("проверяем порядок", "".join(order))
    if ok:
        pass
`)}

Практичнее не печатать все 16 строк, а написать функцию «подходит ли перестановка» и оставить в ответе единственную строку \`print("".join(order))\`.

## Типичные ошибки

- Путают импликацию с «или» без отрицания.
- Игнорируют требование уникальных строк.
- Записывают переменные не в том порядке, который просит бланк.
- Не отличают столбец выражения от столбца переменной.
`,
  }),
  task({
    n: 3,
    label: "база данных",
    readingTime: 8,
    description:
      "Задание 3 ЕГЭ по информатике 2026: поиск в реляционной базе. Файл .ods, связи таблиц, СУММЕСЛИМН и шаблон на Python.",
    keywords: ["база данных ЕГЭ", "ods", "реляционные таблицы"],
    body: `
Задание 3 — базовое, 1 балл. Проверяют поиск в реляционной базе: несколько связанных таблиц, отбор строк и сумма или количество. Специализированное ПО нужно.

**Изменение 2026:** файл приходит в формате \`.ods\`, а не Excel. Открывайте его в LibreOffice Calc. Горячие клавиши похожи на Excel, но не те же. Разделитель аргументов функций в русской локали — точка с запятой.

## Как устроена база

Обычно три таблицы:

- справочник товаров: артикул, название, цена или категория;
- справочник магазинов или районов;
- таблица движений: дата, магазин, артикул, количество.

Связь идёт по коду, а не по названию. Сначала найдите поле, которым таблицы соединяются, и только потом считайте сумму.

## Конспект

1. Откройте все листы. Выпишите, какой код на что ссылается.
2. Переведите вопрос на фильтры: район, даты «с … по …», категория, цена выше порога. Границы дат включают или нет — смотрите формулировку «с … по …» и примеры.
3. Не суммируйте количество, если спрашивают стоимость. Сначала количество умножают на цену из справочника.
4. В Calc удобны \`СУММЕСЛИМН\` и \`СЧЁТЕСЛИМН\`. Если фильтров много, сначала соберите вспомогательный столбец «подходит» функцией \`ЕСЛИ\`, затем суммируйте его.
5. Проверьте на двух-трёх строках вручную, потом смотрите итог.

Связать таблицы можно \`ВПР\` / \`ПРОСМОТРX\`. Если \`ВПР\` не находит код, часто он записан как текст в одной таблице и как число в другой.

## Шаблон на Python

На экзамене быстрее формула в Calc. Шаблон пригодится, если вы выгрузили листы в CSV или вставили строки. Деньги и количества храните числами, не строками.

${py(`
# данные уже сведены в список движений
# price — цена из справочника, qty — количество, district и kind — фильтры
rows = [
    {"district": "Заречный", "kind": "молоко", "price": 80, "qty": 12, "date": "2026-03-01"},
    {"district": "Центр", "kind": "молоко", "price": 90, "qty": 4, "date": "2026-03-02"},
]

total = 0
for r in rows:
    if r["district"] != "Заречный":
        continue
    if r["kind"] != "молоко":
        continue
    if not ("2026-03-01" <= r["date"] <= "2026-03-31"):
        continue
    total += r["price"] * r["qty"]
print(total)
`)}

## Типичные ошибки

- В сумму попали лишние районы или забыт один артикул из нужной категории.
- Сложили количество, а в ответ нужна стоимость.
- Двойная строка после связи «один ко многим» посчитана дважды.
- Фильтр по дате обрезал первый или последний день.

Итог из Calc и из короткой проверки на выборке должны совпасть. Если нет — ошибка в условии отбора, а не в арифметике.
`,
  }),
  task({
    n: 4,
    label: "кодирование и условие Фано",
    readingTime: 8,
    description:
      "Задание 4 ЕГЭ по информатике 2026: неравномерные коды и условие Фано. Как декодировать сообщение и подобрать минимальное кодовое слово.",
    keywords: ["условие Фано", "декодирование", "неравномерный код"],
    body: `
Задание 4 — базовое, 1 балл, без файла. Проверяют кодирование и декодирование. Главный инструмент — условие Фано: ни одно кодовое слово не является началом другого. Тогда строку из нулей и единиц можно декодировать однозначно слева направо.

## Конспект

Условие Фано можно проверить деревом. Каждый код — путь от корня: 0 — левая ветка, 1 — правая. Кодовое слово должно заканчиваться в листе. Если чья-то буква стоит на пути к другой букве, код нефановский.

Типовые сюжеты:

- даны коды нескольких букв, нужно декодировать цепочку;
- даны коды части букв, нужно найти самое короткое, затем самое большое или маленькое кодовое слово для оставшейся буквы;
- выбрать набор кодов, который удовлетворяет Фано.

Перебор, который рекомендует ФИПИ: последовательно добавляйте бит и берите первое слово, которое ещё не является началом уже занятых кодов и само их не продолжает. Смотрите, что именно просят: минимальную длину, минимальное числовое значение среди самых коротких или слово целиком.

Нельзя «занять» остаток дерева так, что для ещё одной буквы места не останется. Если букв несколько и коды не заданы, сначала кодируйте те, для которых условие уже почти выбрало слово, и проверяйте, что остальным ещё есть листья.

## Шаблон на Python

Проверка условия Фано и декодирование строки. Для поиска короткого свободного слова перебираются коды в порядке возрастания длины.

${py(`
def is_fano(codes):
    for a in codes:
        for b in codes:
            if a != b and b.startswith(a):
                return False
    return True

def decode(bits, codes):
    inv = {v: k for k, v in codes.items()}
    i = 0
    out = []
    while i < len(bits):
        found = None
        for code, ch in inv.items():
            if bits.startswith(code, i):
                found = (code, ch)
                break
        if not found:
            return None
        out.append(found[1])
        i += len(found[0])
    return "".join(out)

codes = {"A": "00", "B": "01", "C": "1"}
print(is_fano(codes.values()), decode("00101", codes))

used = set(codes.values())

def free_words(max_len=6):
    words = [""]
    for _ in range(max_len):
        nxt = []
        for w in words:
            for bit in "01":
                cand = w + bit
                trial = list(used) + [cand]
                if is_fano(trial):
                    yield cand
                nxt.append(cand)
        words = nxt

print(next(free_words()))
`)}

## Типичные ошибки

- Кодовое слово совпало с началом уже занятого кода.
- Ищут минимальное число, но забывают, что сначала нужна минимальная длина.
- Декодируют справа налево. Фано читается слева направо.
- В ответ записывают слово вместе с лишними буквами, которые из цепочки не следуют.
`,
  }),
  task({
    n: 5,
    label: "исполнение алгоритма",
    readingTime: 8,
    description:
      "Задание 5 ЕГЭ по информатике 2026: формальное исполнение алгоритма над числом. Как восстановить исходное N и проверить ответ на Python.",
    keywords: ["двоичная запись", "алгоритм ЕГЭ", "формальное исполнение"],
    body: `
Задание 5 — базовое, 1 балл. Дан алгоритм на естественном языке или в виде шагов: число переводят в двоичную систему, дописывают биты, иногда удаляют разряды. Нужно найти минимальное или максимальное исходное N, при котором результат R больше, меньше или равен заданному числу.

ПО не требуется, но короткий перебор на Python надёжнее ручного счёта. ФИПИ просит после ответа обязательно проверить, что это N действительно даёт нужный R.

## Конспект

1. Выпишите алгоритм по шагам, как программу: вход N, преобразования, выход R.
2. Заметьте, что сравнивают: само R, количество единиц в R или десятичное значение.
3. Найдите закономерность. Часто результат монотонен: чем больше N, тем больше R, с отдельными скачками на степенях двойки.
4. Даже если увидели формулу, прогоните N в окрестности границы. Первое подходящее и есть минимум.
5. Проверка обязательна: подставьте найденное N в исходные шаги и убедитесь, что соседнее N ещё не подходит.

Частые операции: число единиц в двоичной записи, последний бит (чётность), дописывание двух бит, сумма цифр по модулю 2.

Не путайте «количество единиц чётно» и «число чётное». Это разные условия. Чётность числа определяется последним битом.

## Шаблон на Python

${py(`
def algo(n):
    s = bin(n)[2:]          # двоичная запись без префикса 0b
    if s.count("1") % 2 == 0:
        s = s + "00"
    else:
        s = s + "11"
    return int(s, 2)

limit = 100
best = None
for n in range(1, 10000):
    r = algo(n)
    if r > limit:
        best = n
        break
print(best, algo(best))
print("проверка соседа:", best - 1, algo(best - 1))
`)}

Замените \`algo\` на шаги из варианта. Если алгоритм зависит от последнего бита, пишите \`n % 2\`, а не \`s.count("1") % 2\`.

## Типичные ошибки

- В двоичную запись попал префикс \`0b\`.
- Ищут любое подходящее N, а не минимальное.
- Не проверяют ответ обратной подстановкой.
- Сравнивают строковую длину, хотя нужно числовое значение R.
`,
  }),
  task({
    n: 6,
    label: "исполнитель Черепаха",
    readingTime: 9,
    description:
      "Задание 6 ЕГЭ по информатике 2026: исполнитель Черепаха и точки с целочисленными координатами. Конспект и шаблон без модуля turtle.",
    keywords: ["черепаха ЕГЭ", "целочисленные точки", "исполнитель"],
    body: `
Задание 6 — базовое, 1 балл. Нужно определить результат работы исполнителя с ограниченным набором команд, чаще всего Черепахи: «вперёд n», «направо m», «налево m», цикл «повтори». Спрашивают, сколько точек с целочисленными координатами лежит внутри фигуры, на границе или и внутри, и на границе.

Модуль \`turtle\` на экзамене может не показать окно. Надёжнее перевести команды в координаты.

## Конспект

1. Черепаха обычно смотрит вдоль положительной оси Y, если в условии не сказано иное. В части вариантов старт — направление вправо. Читайте первую строку условия и не подставляйте «привычное» направление.
2. Команда «направо 90» поворачивает по часовой стрелке. После четырёх таких поворотов направление исходное.
3. Выпишите вершины. Часто получается прямоугольник, прямоугольный треугольник или фигура из двух прямоугольников.
4. Для прямоугольника со сторонами a и b, стороны параллельны осям, точки строго внутри: \`(a - 1) * (b - 1)\`. Если стороны не целые, сначала определите, какие целые x и y попадают в интервал.
5. Если граница учитывается, формула «минус один» не подходит. Либо добавьте точки сторон, либо переберите целые точки в ограничивающем прямоугольнике и проверьте принадлежность.
6. Цикл «повтори k» дублирует маршрут k раз, но с новым направлением. Рисуйте именно столько сторон, сколько выполнений цикла.

Вопрос «внутри или на границе» и вопрос «только внутри» дают разные ответы. Это самая частая потеря балла.

## Шаблон на Python

Шаблон ведёт перо и считает целые точки внутри многоугольника, включая границу. Направление 0 — вверх, 1 — вправо, 2 — вниз, 3 — влево. Подставьте команды своего варианта и флаг \`border\`.

${py(`
points = [(0, 0)]
x = y = 0
direction = 0  # 0 вверх, 1 вправо, 2 вниз, 3 влево
dirs = [(0, 1), (1, 0), (0, -1), (-1, 0)]

def forward(n):
    global x, y
    dx, dy = dirs[direction]
    x += dx * n
    y += dy * n
    points.append((x, y))

def right(deg):
    global direction
    direction = (direction + deg // 90) % 4

def left(deg):
    right(360 - deg)

# пример: повтори 4 [вперёд 5 направо 90]
for _ in range(4):
    forward(5)
    right(90)

def on_border(px, py):
    for (x1, y1), (x2, y2) in zip(points, points[1:]):
        if x1 == x2 == px and min(y1, y2) <= py <= max(y1, y2):
            return True
        if y1 == y2 == py and min(x1, x2) <= px <= max(x1, x2):
            return True
    return False

def inside(px, py):
    # луч вправо, чётность пересечений; подходит для простого многоугольника
    cnt = 0
    for (x1, y1), (x2, y2) in zip(points, points[1:]):
        if (y1 > py) != (y2 > py):
            xint = x1 + (py - y1) * (x2 - x1) / (y2 - y1)
            if px < xint:
                cnt += 1
    return cnt % 2 == 1

xs = [p[0] for p in points]
ys = [p[1] for p in points]
ans = 0
for px in range(min(xs), max(xs) + 1):
    for py in range(min(ys), max(ys) + 1):
        if inside(px, py) or on_border(px, py):
            ans += 1
print(ans, points)
`)}

Если стороны не параллельны осям, проверку границы усложните: точка лежит на отрезке, если расстояние до прямой почти ноль и проекция попадает внутрь отрезка. Для типового прямоугольника достаточно формулы.

## Типичные ошибки

- Неверное начальное направление.
- «Направо» посчитано против часовой стрелки.
- Граница включена, хотя условие просит внутренние точки.
- Команда «вперёд» применена до поворота, хотя в цикле порядок другой.
`,
  }),
  task({
    n: 7,
    label: "объём графики и звука",
    readingTime: 7,
    description:
      "Задание 7 ЕГЭ по информатике 2026: объём растрового изображения и звукового файла. Формулы, степени двойки и частые ошибки в единицах.",
    keywords: ["объём изображения", "звук ЕГЭ", "глубина цвета"],
    body: `
Задание 7 — базовое, 1 балл, без файла. Считают, сколько памяти занимает изображение или звук, либо восстанавливают один параметр, если остальные известны.

Если выкладки разрастаются в огромные числа, ФИПИ советует остановиться и свернуть их в степени двойки. Так и задумано.

## Конспект: изображение

Объём в битах:

\`I = ширина × высота × i\`

где \`i\` — глубина кодирования цвета в битах на пиксель.

- Если цветов N и палитра равномерная, \`i = ceil(log2(N))\`. Для 256 цветов это 8 бит, для 16 — 4.
- Если глубина уже дана, логарифм не нужен.
- Цвет True Color часто 24 бита, иногда указано 32.
- Чтобы получить байты, делят на 8. Килобайт в этих задачах — 1024 байта, мегабайт — 1024 Кбайт, если не сказано иное.

Разрешение в dpi и физический размер в сантиметрах встречаются реже. Сначала переведите размер в число пикселей, потом применяйте формулу. Не подставляйте сантиметры вместо пикселей.

Если спрашивают, во сколько раз уменьшился объём, сократите дробь до формулы. Одинаковые множители сокращаются, считать абсолютные байты не нужно.

## Конспект: звук

\`I = частота × время × глубина × каналы\`

- частота — в герцах, то есть отсчётах в секунду;
- время — в секундах;
- глубина — в битах на отсчёт;
- стерео — 2 канала, моно — 1.

Снова переведите биты в нужную единицу в самом конце. Смешение «бит» и «байт» в середине формулы — типичный промах.

## Шаблон на Python

${py(`
import math

def image_bytes(width, height, colors=None, bits=None):
    if bits is None:
        bits = math.ceil(math.log2(colors))
    return width * height * bits // 8

def sound_bytes(rate, seconds, bits, channels):
    return rate * seconds * bits * channels // 8

print(image_bytes(1024, 768, colors=256))
print(sound_bytes(44100, 60, 16, 2))
`)}

Не целочисленное деление применяйте только когда объём гарантированно кратен 8. Если по условию нужно округление вверх до байта, на один объект пишут \`math.ceil(bits / 8)\`, а не \`bits // 8\`.

## Типичные ошибки

- Забыли второй канал у стерео.
- Взяли 1000 вместо 1024.
- Для 10 цветов поставили 3 бита, хотя \`2^3 = 8 < 10\`, нужно 4.
- Сравнили два объёма, не переведя оба в биты.
`,
  }),
  task({
    n: 8,
    label: "количество информации и слова",
    readingTime: 8,
    description:
      "Задание 8 ЕГЭ по информатике 2026: слова в алфавитном порядке и измерение информации. Нумерация с единицы и шаблон перебора на Python.",
    keywords: ["комбинаторика ЕГЭ", "алфавитный порядок", "количество слов"],
    body: `
Задание 8 — базовое, 1 балл. Проверяют методы измерения количества информации и перечисление слов. Спрашивают, сколько слов удовлетворяет условию, либо каким по счёту стоит данное слово в списке.

ФИПИ напоминает: если слова нумеруют с единицы, числу 0 в системе счисления соответствует первое слово. Сдвиг на единицу — самая дорогая ошибка в этой линии.

## Конспект

Алфавит из N символов, длина слова k, без ограничений: \`N^k\` слов.

Ограничения снимают перебором или формулами:

- ровно одна заданная буква: выбрать позицию и заполнить остальные любыми кроме неё, затем проверить, что эта буква не продублировалась;
- буквы не повторяются: размещения \`N! / (N - k)!\`;
- две одинаковые подряд запрещены: первая буква — N вариантов, каждая следующая — N − 1;
- лексикографический номер: идите слева направо. Для каждой позиции прибавляйте, сколько слов начинается на меньшую букву и дальше идут любые допустимые.

Нумерация: если список с 1, номер слова равен количеству слов строго перед ним плюс 1. Если алфавит сопоставлен цифрам системы счисления, первое слово — это ноль, а не единица. Перед ответом перечитайте, с какого номера идёт список в условии.

Буквы сравнивают в порядке, данном в задании, а не в порядке русского алфавита, если задан свой набор.

## Шаблон на Python

${py(`
from itertools import product

alphabet = "АБВГ"  # порядок важен: он и есть алфавитный
k = 4

words = ["".join(w) for w in product(alphabet, repeat=k)]
# product уже идёт в лексикографическом порядке алфавита

def good(w):
    return w.count("А") == 1 and "ББ" not in w

filtered = [w for w in words if good(w)]
print("количество:", len(filtered))

target = "ВААА"
# номер с 1 среди всех слов:
print("номер среди всех:", words.index(target) + 1)
# номер с 1 среди подходящих:
print("номер среди подходящих:", filtered.index(target) + 1)
`)}

Если длина и алфавит дают больше нескольких миллионов слов, не стройте список целиком. Считайте префиксы циклами или рекурсией с отсечением.

## Типичные ошибки

- Первое слово получило номер 0, хотя в условии нумерация с 1.
- Алфавит отсортирован «по-русски», а в условии другой порядок букв.
- Слово с двумя «А» попало в отбор «ровно одна А», потому что условие проверили только для первой позиции.
- Посчитали слова длины меньше k, хотя звёздочка в этом задании не используется. Здесь длина обычно фиксирована.
`,
  }),
  task({
    n: 9,
    label: "электронная таблица",
    readingTime: 8,
    description:
      "Задание 9 ЕГЭ по информатике 2026: отбор строк в таблице .ods. Формулы LibreOffice Calc и шаблон проверки строки на Python.",
    keywords: ["электронные таблицы", "задание 9", "LibreOffice Calc"],
    body: `
Задание 9 — базовое, 1 балл, но по статистике ФИПИ его плохо решают даже те, кто справляется с другими базовыми задачами. ПО обязательно.

**В 2026 году файл только \`.ods\`.** Откройте его в LibreOffice Calc. Формулы называются по-русски, аргументы разделяются точкой с запятой: \`=СЧЁТЕСЛИ(A2:F2;A2)\`.

## Как выглядит условие

В каждой строке несколько натуральных чисел, обычно 5–7. Нужно посчитать строки, в которых выполнены оба условия, либо взять номер строки, сумму чисел в ней, минимум или максимум. Условия комбинируют повторы, среднее, минимум и максимум.

Примеры формулировок, не из конкретного варианта, а типы:

- в строке ровно три одинаковых числа, остальные различны;
- среднее неповторяющихся больше среднего повторяющихся;
- сумма двух наибольших меньше суммы остальных.

## Конспект

1. Не считайте в уме. Напишите формулу в соседнем столбце и протяните её.
2. Разберите условие на два столбца-флага, затем \`=И(H2;I2)\`.
3. Полезные функции: \`СЧЁТЕСЛИ\`, \`МАКС\`, \`МИН\`, \`СРЗНАЧ\`, \`СУММ\`, \`ЕСЛИ\`.
4. «Все различны» — максимальное значение \`СЧЁТЕСЛИ\` по ячейкам строки равно 1.
5. «Ровно одно число повторяется k раз» проверяйте отдельно: не перепутайте «есть тройка одинаковых» и «есть тройка, а остальные все разные».
6. Если просят номер строки, не забудьте, что данные могут начинаться со второй строки листа, а в задании нумерация идёт по строкам таблицы. Сверьте заголовок.
7. Отсортируйте отфильтрованные строки, только если нужен минимум суммы. Сортировка всего листа без фиксации номера ломает ответ, когда спрашивают исходный номер.

## Шаблон на Python

Если из Calc сохранили CSV без заголовка, тот же критерий можно прогнать скриптом. На экзамене формула обычно быстрее, шаблон нужен как запасной и для тренировки.

${py(`
from collections import Counter

def good(nums):
    c = Counter(nums)
    # тип: ровно три равных, остальные встречаются один раз
    triple = sum(1 for v in c.values() if v == 3) == 1
    rest_unique = all(v in (1, 3) for v in c.values())
    only_one_repeat = list(c.values()).count(3) == 1 and 2 not in c.values()
    cond1 = triple and rest_unique and only_one_repeat
    avg = sum(nums) / len(nums)
    cond2 = max(nums) + min(nums) > avg  # замените на условие варианта
    return cond1 and cond2

rows = [
    [1, 1, 1, 2, 3],
    [4, 4, 4, 4, 5],
    [7, 7, 7, 8, 9],
]
print(sum(1 for r in rows if good(r)))
print(max(i + 1 for i, r in enumerate(rows) if good(r)))
`)}

Логические функции \`И\`, \`ИЛИ\`, \`НЕ\` в Calc — частый источник ошибок из методички ФИПИ. В формуле пишите \`И(A2;B2)\`, а не \`A2 И B2\`.

## Типичные ошибки

- Условие «оба» заменено на «хотя бы одно».
- Среднее посчитано по уникальным значениям, хотя нужно по всем ячейкам, или наоборот.
- Номер строки взят после сортировки.
- Файл открыт не в том редакторе, и \`.ods\` сохранился с потерянными формулами.
`,
  }),
  task({
    n: 10,
    label: "поиск в тексте",
    readingTime: 6,
    description:
      "Задание 10 ЕГЭ по информатике 2026: поиск слова в документе .odt. Целое слово, регистр и словоформы в LibreOffice Writer.",
    keywords: ["поиск в тексте", "odt", "текстовый процессор"],
    body: `
Задание 10 — базовое, 1 балл. Нужно найти, сколько раз в документе встречается слово или сочетание. ПО обязательно.

**В 2026 году файл только \`.odt\`,** открывается в LibreOffice Writer. Поиск «глазами» по длинному тексту почти всегда даёт ошибку.

## Конспект

1. Откройте поиск: Ctrl+F, затем дополнительные параметры.
2. Включите «только слово целиком», если нельзя считать вхождения внутри других слов. «Кот» не должен находиться в «который» и «котлета».
3. Регистр: если написано «именно в таком виде» или слово дано с заглавной, включите учёт регистра. Если сказано «независимо от регистра» — выключите.
4. Словоформы. «Река» и «реки» — разные слова, если ищут конкретную форму. ФИПИ фиксирует обе ошибки: лишние словоформы и пропуск подходящих.
5. Смотрите дефис, «ё/е» и кавычки. В условии обычно сказано, считать ли дефисное написание.
6. Число замен в «найти и заменить» должно совпасть со счётчиком «найти все». Если заменяете на себя, документ не испортится, а число покажет счёт.

Не используйте поиск операционной системы по файлу, если редактор и ОС по-разному режут слова. На экзамене инструмент — текстовый процессор.

## Шаблон на Python

На экзамене ищите в Writer. Скрипт нужен, чтобы на тренировке проверить логику «целое слово». Скопируйте текст документа в файл или вставьте строку.

${py(`
import re

text = open("task10.txt", encoding="utf-8").read()
word = "Река"
# границы слова; для русских букв \\b ненадёжен, задайте границы явно
pattern = r"(?<![А-Яа-яЁёA-Za-z])" + re.escape(word) + r"(?![А-Яа-яЁёA-Za-z])"
print(len(re.findall(pattern, text)))
`)}

Если условие про регистр иное, приведите и текст, и образец к одному регистру до поиска. Для словоформ перечисляйте только те формы, которые разрешены условием, а не все падежи.

## Типичные ошибки

- Вхождения внутри длинных слов.
- Посчитаны «реки», «реку», хотя нужна только «река».
- Пропущены слова в начале предложения с заглавной буквы, когда регистр не важен.
- Поиск по части текста, а не по всему документу, включая колонтитулы, если условие просит весь файл. Обычно нужен основной текст — прочитайте абзац условия про область поиска.
`,
  }),
  task({
    n: 11,
    label: "информационный объём сообщения",
    readingTime: 7,
    description:
      "Задание 11 ЕГЭ по информатике 2026: пароли, идентификаторы и объём в байтах. Как округлять биты и не потерять пользователя.",
    keywords: ["объём сообщения", "пароль", "алфавитный подход"],
    body: `
Задание 11 — повышенный уровень, 1 балл, без отдельного файла. Считают, сколько бит или байт нужно на пароль, регистрационный номер и запись пользователя, затем — на всех пользователей.

ФИПИ подчёркивает два округления, их нельзя смешивать:

- на символ пароля выделяют одинаковое и минимально возможное **целое число бит**;
- на пароль или запись целиком — одинаковое и минимально возможное **целое число байт**.

## Конспект

Алфавит из N символов: мощность одного символа \`i = ceil(log2(N))\` бит. Нельзя хранить символ в дробном числе бит.

Пароль длины L: \`L * i\` бит. Если дальше объём переводят в байты, округляют вверх до целого байта: \`ceil(L * i / 8)\` байт на один пароль. Округляют запись целиком, а не каждый символ до байта, если в условии не сказано иначе.

Идентификатор. Если пользователей M, на номер нужно \`ceil(log2(M))\` бит, затем тоже обычно целое число байт на поле. M пользователей требуют различения M номеров, значит 0 … M−1, и \`log2(M)\` округляют вверх. Для 1000 пользователей это 10 бит, не 9: \`2^9 = 512 < 1000\`.

Общий объём: \`число пользователей × байт на запись\`. Переведите в Кбайт через 1024, если просят.

Дополнительные поля складывают в битах, и только потом всю запись поднимают до байта — либо каждое поле до байта, если сказано «каждое поле хранится отдельно в целом числе байт». Эту фразу надо найти в условии до счёта.

## Шаблон на Python

${py(`
import math

def bits_for_alphabet(n):
    return math.ceil(math.log2(n))

def bytes_for_bits(bits):
    return math.ceil(bits / 8)

alphabet = 12          # символов в пароле
length = 10
users = 2000

pass_bits = length * bits_for_alphabet(alphabet)
id_bits = math.ceil(math.log2(users))
record_bits = pass_bits + id_bits
record_bytes = bytes_for_bits(record_bits)
print(record_bytes, users * record_bytes)
`)}

Подставьте, округляется ли каждое поле отдельно. Если да, \`bytes_for_bits\` вызывают для пароля и для идентификатора по отдельности и результаты складывают.

## Типичные ошибки

- Символ положили в целый байт без требования.
- Для 1000 объектов взяли 9 бит.
- Перепутали, что округляется: пароль или вся база.
- Использовали 1000 вместо 1024 при переводе в килобайты.
`,
  }),
  task({
    n: 12,
    label: "исполнитель МТ",
    readingTime: 10,
    description:
      "Задание 12 ЕГЭ по информатике 2026: новый исполнитель МТ. Лента, состояния, команды L, R, N, S. Как исполнять вручную, без программы.",
    keywords: ["исполнитель МТ", "машина Тьюринга", "задание 12"],
    body: `
В 2026 году это главное содержательное изменение демоверсии. Линия осталась той же: умение исполнить алгоритм исполнителя с фиксированным набором команд. Уровень — повышенный, 1 балл, файл не нужен.

Новый сюжет — исполнитель **МТ**. ФИПИ прямо не советует решать его программой: легко потратить время и ошибиться в коде. Надёжный путь — несколько тактов вручную и формулировка, что машина делает с лентой.

В открытом банке прошлых лет ещё встречаются старые сюжеты: исполнитель Редактор со строкой «замени / нашлось». Их тоже стоит уметь, но на экзамене 2026 ориентируйтесь на МТ из демоверсии.

## Как устроен МТ

Есть бесконечная лента из ячеек. В ячейке один символ алфавита, включая пустой «λ». Головка видит одну ячейку.

Программа — таблица. Строки — состояния q0, q1, … Столбцы — символы. В клетке команда из трёх частей:

\`символ, сдвиг, новое состояние\`

- первый элемент записывают в текущую ячейку, даже если он совпадает со старым;
- затем головка сдвигается: \`L\` влево, \`R\` вправо, \`N\` остаётся, \`S\` машина останавливается после этой команды;
- третий элемент — новое состояние, внутренняя память машины.

Пустая клетка таблицы означает, что такая пара «состояние + символ» в программе не задана. На экзамене до неё обычно не доходят, если следовать условию.

Состояние — это не «содержимое ленты». Лента — внешняя память, состояние — внутренняя. Одна и та же единица на ленте в q1 и в q2 обрабатывается разными командами.

## Конспект

1. Найдите, где головка в начале и какое состояние. В демоверсии она часто стоит в ближайшей ячейке справа от последовательности, состояние q0.
2. Выполните 4–8 тактов на короткой ленте, например из четырёх символов. Рисуйте ленту и отмечайте головку стрелкой.
3. Сформулируйте словами, что делает машина: идёт влево и меняет нули на единицы до первой единицы, эту единицу гасит и останавливается, и тому подобное. Если фраза короткая и совпала с тактами — программа понята.
4. Дальше не моделируйте 1000 шагов. Примените правило к условию: сколько нулей останется, каким станет двоичное число, какое исходное число нулей максимально.
5. Разберите край: головка сразу видит λ, встречает сначала 1, в ленте нет нужной цифры. Краевые случаи часто и дают ответ.
6. Если спрашивают минимум или максимум исходного количества нулей, сначала опишите все ленты, которые после работы дают нужный результат, потом выберите экстремум.

Команда \`0, L, q3\` читается так: записать 0, сдвинуться влево, перейти в q3. Порядок именно такой: сначала запись, потом сдвиг.

## Зачем не писать программу

Симулятор ниже годится только для тренировки на короткой ленте, чтобы сверить своё понимание таблицы. На экзамене перенос таблицы в код занимает дольше, чем ручной разбор, и одна перепутанная запятая даёт неверный ответ. ФИПИ рекомендует ручное исполнение.

${py(`
# учебный симулятор, не для бланка экзамена
program = {
    ("q0", "λ"): ("λ", "L", "q1"),
    ("q1", "0"): ("1", "L", "q1"),
    ("q1", "1"): ("0", "N", "STOP"),
    ("q1", "λ"): ("λ", "N", "STOP"),
}

tape = {i: ch for i, ch in enumerate("1011")}
head = max(tape) + 1
state = "q0"
steps = 0
while state != "STOP" and steps < 10000:
    ch = tape.get(head, "λ")
    write, move, state = program[(state, ch)]
    tape[head] = write
    if move == "L":
        head -= 1
    elif move == "R":
        head += 1
    steps += 1

lo, hi = min(tape), max(tape)
print("".join(tape.get(i, "λ") for i in range(lo, hi + 1)))
`)}

Старый сюжет «Редактор», если попадётся в банке: пока в строке нашлось сочетание, заменяйте его. Цикл \`while\` и \`str.replace\` с лимитом 1. Следите, с начала строки или с конца ищет «нашлось».

## Типичные ошибки

- Сдвиг сделали до записи символа.
- \`S\` приняли за «сдвиг», а не за останов.
- Пустой символ λ обработали как 0.
- Смоделировали только середину ленты и потеряли выход головки за край последовательности.
`,
  }),
  task({
    n: 13,
    label: "маска подсети",
    readingTime: 8,
    description:
      "Задание 13 ЕГЭ по информатике 2026: IP-адрес и маска подсети. Чётность адреса, число единиц в двоичной записи и шаблон на Python.",
    keywords: ["маска подсети", "IP-адрес", "сети ЕГЭ"],
    body: `
Задание 13 — повышенный уровень, 1 балл. Нужно уметь получать адрес сети по IP и маске, перечислять адреса узлов и работать с двоичной и шестнадцатеричной записью. Файла нет. В кодификаторе 2026 тему относят к блоку кодирования, по сути линия та же.

## Конспект

IP и маска — 32 бита, четыре октета 0–255.

Маска в задачах ЕГЭ непрерывна: слева единицы, справа нули. Запись \`/k\` означает k единиц. \`255.255.255.0\` — это \`/24\`.

Адрес сети: побитовое И адреса и маски. Два узла в одной сети, если после И с одной и той же маской адреса совпали.

Число адресов в сети с маской \`/k\` равно \`2^(32-k)\`. Если спрашивают число узлов и явно исключают адрес сети и широковещательный, вычитают 2. Если спрашивают «сколько различных адресов» — читайте, входят ли служебные. Не вычитайте 2 по привычке.

ФИПИ отдельно отмечает путаницу:

- чётное двоичное число — последний бит 0;
- чётное количество единичных разрядов — чётная сумма битов, это не то же самое.

\`1110₂\` чётно, но единиц в нём три.

Шестнадцатеричную запись переводите потетрадно: одна hex-цифра — четыре бита. Не переводите всё число в десятичное, если вопрос про биты маски.

## Шаблон на Python

Модуль \`ipaddress\` входит в стандартную библиотеку Python, дополнительные пакеты не нужны.

${py(`
import ipaddress

ip = ipaddress.ip_address("192.168.32.160")
mask_prefix = 26
net = ipaddress.ip_network((ip, mask_prefix), strict=False)
print(net.network_address)
print(net.num_addresses)

# сколько адресов сети имеют чётное число единиц в двоичной записи
cnt_even_bits = 0
cnt_even_value = 0
for addr in net:
    bits = bin(int(addr)).count("1")
    if bits % 2 == 0:
        cnt_even_bits += 1
    if int(addr) % 2 == 0:
        cnt_even_value += 1
print(cnt_even_bits, cnt_even_value)
`)}

Если нужно найти минимальную подходящую маску, перебирайте префикс от 0 до 32 и проверяйте условие на всех адресах сети или выведите его из битов. Для «все узлы сети имеют …» условия, которое зависит только от младших битов, перебор 2^n при n ≤ 16 мгновенный. При большей сети считайте по разрядам, не перечисляйте 2^24 адресов зря.

## Типичные ошибки

- Перепутаны чётность числа и чётность числа единиц.
- Вычли 2, хотя вопрос про все адреса.
- Маска с «дыркой» в единицах, так на ЕГЭ не бывает, и если получилась — ошибка в переводе.
- Побитовое И заменено сложением октетов.
`,
  }),
  task({
    n: 14,
    label: "системы счисления",
    readingTime: 8,
    description:
      "Задание 14 ЕГЭ по информатике 2026: позиционные системы счисления. Перевод, уравнения в разных основаниях и перебор на Python.",
    keywords: ["системы счисления", "основание", "перевод чисел"],
    body: `
Задание 14 — повышенный уровень, 1 балл. Числа и выражения записаны в разных системах счисления. Нужно найти основание, цифру или число подходящих оснований. Файла нет.

ФИПИ советует начинать с перевода в одну систему, обычно в ту, у которой основание меньше. Типичная ошибка — арифметика в недесятичной системе: вычитание единицы из \`1010000₂\` «в столбик» с переносом.

## Конспект

Цифра не может быть больше или равна основанию. Если в записи есть цифра \`C\` или \`x\`, основание больше этой цифры. Буквы: A=10, B=11, … F=15. В основаниях больше 36 стандартных цифр нет, такие основания на ЕГЭ редки и задаются явно.

Перевод в десятичную: \`d_n ... d0\` в основании p равно \`d_n * p^n + ... + d0\`.

Обратный перевод: деление на основание с остатком, остатки пишут снизу вверх.

Типовые вопросы:

- найти основание p, при котором выражение равно данному числу;
- найти число решений уравнения относительно основания или цифры;
- посчитать, сколько чисел в данном основании обладают свойством.

Если уравнение содержит неизвестное основание p, переберите p от «максимальная цифра + 1» до разумной границы. Часто основание не больше 36, иногда до нескольких сотен — тогда не стройте строку, а считайте многочлен.

Прямое вычитание в двоичной системе отрабатывайте отдельно: \`1000₂ − 1 = 111₂\`, не \`0111\` с потерей разряда.

## Шаблон на Python

\`int(строка, основание)\` работает для оснований от 2 до 36. Для неизвестной цифры x перебирайте её как число и собирайте значение многочленом, не через буквы.

${py(`
def from_base(digits, base):
    value = 0
    for d in digits:
        if d >= base:
            return None
        value = value * base + d
    return value

# уравнение вида 12x45_p = 6x7_p + число, цифры задайте числами
answers = []
for base in range(6, 100):  # основание больше любой цифры
    left = from_base([1, 2, 5, 4, 5], base)       # пример записи
    right = from_base([6, 5, 7], base)
    if left is None or right is None:
        continue
    if left == right + 100:
        answers.append(base)
print(answers)
print(int("101011", 2), bin(43)[2:])
`)}

## Типичные ошибки

- Основание не больше максимальной цифры, и запись всё равно принята.
- \`int("12", 2)\` вызван для цифр больше 1 — будет исключение, его надо отсечь заранее.
- Забыт перенос при вычитании единицы в двоичной записи.
- В ответ попали все основания, а просили минимальное.
`,
  }),
  task({
    n: 15,
    label: "логическое выражение",
    readingTime: 9,
    description:
      "Задание 15 ЕГЭ по информатике 2026: выражение, истинное при любом x. Отрезки, делимость, импликация и перебор на Python.",
    keywords: ["логика ЕГЭ", "импликация", "отрезок"],
    body: `
Задание 15 — повышенный уровень, 1 балл. Дано логическое выражение. Нужно найти такие A, при которых оно тождественно истинно на всей области, либо наименьшую длину отрезка A, наибольшее число A и похожие величины.

ФИПИ советует помнить, что импликация \`P → Q\` равна \`¬P ∨ Q\`. Выражение должно быть истинно при всех допустимых x, а не на одном удачном наборе.

## Конспект

Два частых вида.

**Числовое множество A.** Выражение вида «если x принадлежит отрезку P, то x принадлежит A» или с эквиваленцией. A восстанавливают по отрезкам: смотрите, какие x обязаны входить в A и какие не могут входить. Длина — разность концов. Если A — целочисленное множество, длина и мощность — разные вещи. Читайте, что спрашивают.

**Делимость и побитовые операции.** Обозначения \`ДЕЛ(n, m)\` — n делится на m без остатка. Выражение должно быть верно для всех натуральных x из указанного диапазона. Тогда A перебирают, а для каждого A проверяют все x.

Порядок:

1. Замените импликацию на «не первое или второе».
2. Найдите область x. Если это все целые от 1 до 1000 или от 0 до 10^4, хватает перебора. Если все действительные — рисуйте отрезки, перебор точек даст дыры.
3. Для каждого кандидата A проверьте, что нет ни одного x-контрпримера.
4. Среди подходящих A выберите минимум, максимум или кратчайший отрезок, как сказано.

Тождественная истина не следует из истинности на двух-трёх «подозрительных» точках. Контрпример один, и гипотеза снята.

## Шаблон на Python

${py(`
def delit(n, m):
    return m != 0 and n % m == 0

def impl(a, b):
    return (not a) or b

def ok(a):
    for x in range(1, 1000):
        expr = impl(delit(x, a) or (x > 70), delit(x, 22) or (x > 50))
        if not expr:
            return False
    return True

good = [a for a in range(1, 200) if ok(a)]
print(good[:10], "min =", min(good) if good else None)
`)}

Подставьте выражение варианта. Границы цикла должны покрывать все x, от которых зависит условие. Если в выражении есть порог 70, проверяйте вокруг него и далеко за ним, не только сам порог.

Для отрезков на прямой Python нужен реже: набросайте на черновике, какие куски обязаны лежать в A. Если концы не целые, не округляйте их до перебора.

## Типичные ошибки

- Проверили несколько x и решили, что истинно при всех.
- Импликация ложна на «ложь → истина». Она ложна только на «истина → ложь».
- Минимальная длина отрезка заменена на число целых точек.
- Пропущен x = 0 или отрицательные, если область их включает.
`,
  }),
  task({
    n: 16,
    label: "рекурсия",
    readingTime: 8,
    description:
      "Задание 16 ЕГЭ по информатике 2026: рекуррентные функции. Как упростить формулу, поставить базу и не упереться в лимит рекурсии.",
    keywords: ["рекурсия ЕГЭ", "мемоизация", "рекуррентная функция"],
    body: `
Задание 16 — повышенный уровень, 1 балл. Дана рекуррентная функция, нужно вычислить F(n) или сумму значений. Программу писать не обязаны, но без мемоизации рекурсия часто не заканчивается вовремя.

ФИПИ приводит ориентир: выражение вида \`N! / (N − 1)!\` не считают рекурсивным факториалом. Сначала упростите. Если программа падает по глубине стека или считает слишком долго, способ неверный, а не «нужно подождать».

## Конспект

1. Выпишите базу: при каких n функция задана числом и рекурсия останавливается.
2. Упростите формулу. Сократите факториалы, заметьте период, сведите два вызова к одному.
3. Если остаётся дерево вызовов с повторами, запоминайте уже посчитанное. Это \`lru_cache\` или список.
4. База должна покрывать все ветки. Если есть \`F(n - 1)\` и \`F(n - 3)\`, база нужна не в одной точке, а на нескольких подряд значениях, иначе уйдёте в отрицательные n.
5. Следите за порядком условий в программе. Сначала отсечение базы, потом рекурсивная ветка. Наоборот — бесконечный вызов.
6. Иногда спрашивают не F(n), а количество единиц при вычислении или сумму F(i) на отрезке. Тогда считайте снизу вверх циклом: это и быстрее, и прозрачнее.

Цикл снизу вверх предпочтительнее рекурсии, если зависимость только от меньших аргументов.

## Шаблон на Python

${py(`
from functools import lru_cache

@lru_cache(None)
def f(n):
    if n < 3:
        return n
    if n % 2 == 0:
        return f(n - 1) + 2 * n
    return f(n - 2) + n

print(f(20))

# тот же счёт циклом, если каждый F зависит только от меньших
N = 20
F = [0] * (N + 1)
for n in range(N + 1):
    if n < 3:
        F[n] = n
    elif n % 2 == 0:
        F[n] = F[n - 1] + 2 * n
    else:
        F[n] = F[n - 2] + n
print(F[N], sum(F))
`)}

Если n большое, а ветки вычитают единицу, глубина рекурсии равна n. Тогда либо цикл, либо \`sys.setrecursionlimit\`. Лимит не лечит экспоненциальное дерево без кэша: сначала кэш, потом лимит.

## Типичные ошибки

- Нет условия остановки для всех веток.
- Кэш забыт, и одинаковые F(k) считаются тысячи раз.
- Упрощение \`n! / (n-1)! = n\` не замечено, программа считает факториал большого n.
- Сумма собрана от 1 до n без тех n, где функция не определена по условию.
`,
  }),
  task({
    n: 17,
    label: "числовая последовательность",
    readingTime: 9,
    description:
      "Задание 17 ЕГЭ по информатике 2026: пары и тройки чисел из файла .txt. Два прохода, индексы и типичные ошибки ввода.",
    keywords: ["задание 17", "пары чисел", "файл txt"],
    body: `
Задание 17 — повышенный уровень, 1 балл. Нужна небольшая программа: прочитать числовую последовательность и обработать пары, тройки или соседние элементы. Отдельного частичного балла нет: ответ целиком должен совпасть с эталоном.

**В 2026 году данные приходят в \`.txt\`.** Не ищите \`xls\`.

Обычно в ответе два числа через пробел: количество подходящих пар и максимальная сумма. Пишите их ровно в том порядке и с тем разделителем, которые указаны в условии.

## Конспект

ФИПИ рекомендует схему из двух проходов:

1. Считать все числа в список. Один раз, без повторного чтения того же файла в середине логики.
2. Первым проходом найти порог, если он зависит от данных: максимальный элемент, оканчивающийся на 13, минимальный положительный и так далее.
3. Вторым проходом перебрать пары \`(a[i], a[i+1])\` или тройки. Не смещайте индекс.
4. Отдельно копить количество и максимум или минимум. Максимум инициализируйте первым подходящим значением или очень маленьким числом, не нулём, если суммы бывают отрицательными. В этой линии числа чаще натуральные, но читайте тип.
5. Строгость сравнения: «больше» и «не меньше» дают разный ответ. «Оканчивается на 13» — это \`abs(x) % 100 == 13\`, не \`% 10\`.

Пара — два соседних элемента, если так сказано. Иногда пара — любые два различных элемента последовательности. Это квадратичный перебор, при длине нескольких тысяч он допустим, при десятках тысяч уже нет. В актуальных вариантах обычно соседние или тройки подряд, длина файла позволяет и квадрат, но не пишите его «на всякий случай», если просят соседей.

## Шаблон на Python

${py(`
nums = [int(x) for x in open("17.txt") if x.strip()]

# порог: например, максимальное число, оканчивающееся на 17
limit = max(x for x in nums if abs(x) % 100 == 17)

count = 0
best = None
for a, b in zip(nums, nums[1:]):
    cond = (a > limit) + (b > limit) == 1  # ровно одно больше порога
    if cond and (a + b) % 2 == 0:
        count += 1
        s = a + b
        if best is None or s > best:
            best = s

print(count, best)
`)}

Для троек цикл \`for a, b, c in zip(nums, nums[1:], nums[2:])\`. Не используйте \`range(len(nums))\` с \`i+2\`, не проверив последний индекс: \`zip\` это уже делает.

Пустые строки в файле отфильтруйте. Повторный \`open\` в цикле не нужен и как раз даёт ошибку «неполного или повторного ввода», о которой пишет ФИПИ.

## Типичные ошибки

- Пара \`(a[i], a[i])\` или пропуск последней пары.
- Порог считается внутри цикла по парам и меняется по ходу.
- Строгое и нестрогое неравенство перепутаны.
- Второе число ответа — номер пары, хотя просили сумму.
- Файл открыт не в той кодировке. Для целых чисел это почти никогда не важно, для смешанного текста — да. Здесь достаточно построчного \`int\`.
`,
  }),
  task({
    n: 18,
    label: "робот и электронная таблица",
    readingTime: 9,
    description:
      "Задание 18 ЕГЭ по информатике 2026: путь Робота по таблице .ods. Динамика «справа и снизу», стены и сбор значений.",
    keywords: ["робот ЕГЭ", "динамическое программирование", "ods"],
    body: `
Задание 18 — повышенный уровень, 1 балл. ПО нужно. Проверяют обработку целочисленных данных в электронной таблице. На практике это поле с числами и Робот, который ходит по клеткам и собирает сумму, минимум или максимум.

**Файл 2026 — \`.ods\`.** Стены и пустые клетки не пропускайте: ФИПИ отмечает, что путь часто моделируют неверно и неверно суммируют «добычу».

## Конспект

Классический маршрут: из левого верхнего угла в правый нижний, ходы только вправо и вниз. Тогда значение клетки:

\`F[r][c] = cell[r][c] + max(F[r-1][c], F[r][c-1])\`

Для минимума — \`min\`. В первую клетку кладут само число, без прибавления «из ниоткуда».

В Calc это формула в соседней области или поверх копии листа. Не затирайте исходные числа, пока не скопировали их.

Если есть стены, в непроходимую клетку ставьте «минус бесконечность» для максимума, чтобы путь туда не зашёл. Не обходите стену ходом, которого нет в условии.

Варианты ходов бывают другими: только влево и вверх, или из правого нижнего угла. Направление читают до формулы. Формула «вправо и вниз» на обратном маршруте даёт чужой ответ.

Клетка старта входит в сумму, если не сказано обратное. То же с финишем.

Иногда два вопроса: максимальная сумма и число клеток на таком пути, либо сумма при обязательном проходе через клетку. Второй вопрос — два пути: старт → точка и точка → финиш. Если лучших путей несколько, дополнительное условие (минимальная длина, конкретная клетка) берут из текста.

## Шаблон на Python

Сохраните лист как CSV или скопируйте числа. Нуль ниже замените на маркер стены, если в условии стена — не ноль, а пустая клетка.

${py(`
grid = [
    [1, 2, 3],
    [4, -1, 5],  # -1 пусть будет стеной
    [6, 7, 8],
]
rows, cols = len(grid), len(grid[0])
INF = 10**18
F = [[-INF] * cols for _ in range(rows)]

def ok(r, c):
    return 0 <= r < rows and 0 <= c < cols and grid[r][c] != -1

if ok(0, 0):
    F[0][0] = grid[0][0]
for r in range(rows):
    for c in range(cols):
        if not ok(r, c):
            continue
        for pr, pc in ((r - 1, c), (r, c - 1)):
            if ok(pr, pc):
                F[r][c] = max(F[r][c], grid[r][c] + F[pr][pc])
print(F[-1][-1])
`)}

Первая клетка не должна получить \`grid + (-INF)\`. В шаблоне она задаётся отдельно, а прибавление идёт только из уже достижимых соседей. Если стартовая клетка ещё раз обновится из недостижимого соседа, \`ok\` это отсекает.

В Calc для клетки C2 при данных в A2 формула вида \`=A2+МАКС(B1;A2)\` легко ссылается не туда. Протяните на две строки и сверьте с ручным счётом угла 2×2.

## Типичные ошибки

- Ход по диагонали, которого нет.
- Стена заменена нулём и по ней прошли.
- В сумму не вошёл старт или финиш.
- Максимум взят по всей таблице, а не по допустимому пути.
`,
  }),
  task({
    n: 19,
    label: "игра: один ход",
    readingTime: 8,
    description:
      "Задание 19 ЕГЭ по информатике 2026: логическая игра, базовый уровень. Как найти позиции, где партия заканчивается за один ход.",
    keywords: ["теория игр", "куча камней", "выигрышная стратегия"],
    body: `
Задания 19, 20 и 21 — одна игра. Меняется вопрос. Задание 19 — базовое, 1 балл. Обычно спрашивают наименьшее значение S, при котором первый игрок не выигрывает сразу, но второй своим первым ходом уже выигрывает при любом ответе первого. Точная формулировка каждый год чуть разная, её надо прочитать до раскраски позиций.

Игроков чаще называют Петя и Ваня. Петя ходит первым. Ходы заданы: добавить 1, умножить на 2, убрать камни и так далее. Игра кончается, когда количество камней стало не меньше N или когда ход невозможен. Победитель — кто сделал последний ход, если не сказано иначе. Две кучи встречаются так же часто, как одна.

## Конспект

Не ищите «красивое» S. Разметьте позиции.

Позиция выигрышная, если из неё есть ход в проигрышную для соперника. Позиция проигрышная, если любой ход ведёт в выигрышную для соперника или ходов нет и по правилам это поражение текущего игрока. В задачах «кто взял последним» конечные позиции, где игра уже закончена предыдущим ходом, текущему не достаются: он их не ходит.

Для задания 19 обычно нужна самая короткая стратегия:

- позиция, из которой можно сразу завершить игру, — победа за один ход;
- позиция, из которой любой ход отдаёт сопернику такую победу, а своего немедленного выигрыша нет, — то, что часто спрашивают в №19.

Выпишите все S в рабочем диапазоне, не только минимум. Задания 20 и 21 используют ту же разметку. Если разобрать 19 заново для каждого номера, легко разойтись в правилах.

Две кучи: позиция — пара \`(a, b)\`. Не забывайте, что ход делается в одной куче, если так сказано. Симметрия \`(a, b)\` и \`(b, a)\` есть не всегда, потому что вопрос может про наименьшую сумму.

## Шаблон на Python

Один параметр S, ходы «+1» и «*2», конец при S ≥ 129. Функция возвращает, выигрывает ли текущий игрок. Для №19 отдельно собраны позиции с немедленной победой и позиции, где любой ход ведёт в немедленную победу соперника.

${py(`
GOAL = 129

def moves(s):
    return [s + 1, s * 2]

win1 = {s for s in range(1, GOAL) if any(m >= GOAL for m in moves(s))}

def is_19(s):
    if s in win1:
        return False
    return all(m in win1 for m in moves(s))

print([s for s in range(1, GOAL) if is_19(s)])
`)}

Подставьте ходы и конец партии из условия. Если кучи две, \`s\` замените на кортеж и генерируйте ходы по каждой куче.

## Типичные ошибки

- Петя и Ваня поменяны местами. Первый ход у Пети.
- В №19 взяли позицию, из которой Петя выигрывает сразу, хотя вопрос про вынужденную победу Вани.
- Забыт один из разрешённых ходов.
- Конец игры «больше N» вместо «не меньше N».
`,
  }),
  task({
    n: 20,
    label: "выигрышная стратегия",
    readingTime: 8,
    description:
      "Задание 20 ЕГЭ по информатике 2026: две позиции, при которых Петя выигрывает вторым ходом. Полный перебор ходов соперника.",
    keywords: ["выигрышная стратегия", "Петя и Ваня", "теория игр"],
    body: `
Задание 20 — повышенный уровень, 1 балл, та же игра, что в №19. Обычно просят два наименьших S, при которых Петя имеет выигрышную стратегию, причём не быстрее своего второго хода. Ответ — два числа в порядке возрастания, без лишних знаков.

Частичного балла нет. Оба числа и порядок должны совпасть с эталоном.

## Конспект

Победа своим вторым ходом означает:

- нельзя выиграть первым ходом;
- существует ход Пети в такую позицию, что при любом ответе Вани у Пети находится ход, заканчивающий игру.

Это не «Петя может выиграть, если Ваня ошибётся». Стратегия учитывает любой ход проигрывающего. ФИПИ пишет, что главная ошибка блока 19–21 — разобрать не все ходы того, кто проигрывает при правильной стратегии.

Порядок разметки:

1. Отметьте позиции победы за 1 ход, как в задании 19.
2. Позиция проигрыша за 1 ход соперника: сама не выигрышная за 1, и каждый ход из неё ведёт в победу за 1.
3. Победа за 2 хода: есть ход в позицию проигрыша соперника, и нет победы за 1.

Для двух куч считайте сумму, если в ответе сумма, и не выводите обе кучи, если бланк ждёт два значения S.

Если подходящих S больше двух, берут два наименьших, если так сказано. Не выводите все.

## Шаблон на Python

${py(`
GOAL = 129

def moves(s):
    return [x for x in (s + 1, s * 2) if x < 300]

def win_in_1(s):
    return any(m >= GOAL for m in moves(s))

def lose_in_1(s):
    if s >= GOAL or win_in_1(s):
        return False
    return all(win_in_1(m) for m in moves(s))

def win_in_2(s):
    if s >= GOAL or win_in_1(s):
        return False
    return any(lose_in_1(m) for m in moves(s))

ans = [s for s in range(1, GOAL) if win_in_2(s)]
print(*ans[:2])
`)}

Ограничение \`x < 300\` не должно отрезать ход, который ещё имеет смысл. Если после умножения получается значение больше цели, оно как раз выигрышное и должно попасть в список ходов. Фильтруйте только совсем далёкие ветки, которые правилами запрещены.

## Типичные ошибки

- Взята позиция, где Ваня может ошибиться, но есть и защитный ход.
- В ответ попало S, где Петя выигрывает уже первым ходом.
- Числа не упорядочены по возрастанию.
- Стратегия проверена только для одного ответа соперника.
`,
  }),
  task({
    n: 21,
    label: "дерево игры",
    readingTime: 8,
    description:
      "Задание 21 ЕГЭ по информатике 2026: дерево партии и позиция, где Ваня выигрывает своим вторым ходом. Когда дерево можно не строить целиком.",
    keywords: ["дерево игры", "стратегия Вани", "неполное дерево"],
    body: `
Задание 21 — повышенный уровень, 1 балл, та же партия. Часто нужно значение S, при котором у Вани есть выигрышная стратегия, а у Пети нет стратегии выиграть раньше, чем за два своих хода, и спрашивают конкретное S или два значения.

Если ответ не виден из решения №20, ФИПИ рекомендует дерево: полное или неполное, схема или таблица на черновике. Неполное дерево допустимо, только если вы осознанно отсекаете ветки, которые не меняют вывод. «Не смотрел вторую кучу» — не отсечение, а ошибка.

## Конспект

Вопрос №21 обычно на ход длиннее, чем №20. Ваня выигрывает своим вторым ходом, когда:

- Петя не может закончить игру раньше;
- как бы Петя ни ходил первым ходом, Ваня может ответить так, чтобы дальше у Пети не было спасения, и Ваня завершит игру на своём втором ходу.

Слова «при любом ходе Пети» и «Ваня может выбрать» не меняются местами. Существование хода у проигрывающего не спасает его, если есть и плохие ходы: смотрят, может ли он выбрать хороший. У выигрывающего достаточно одного правильного ответа на каждый чужой ход.

Неполное дерево: сначала выпишите ходы Пети из стартового S. Если хотя бы на один ход Пети у Вани нет нужного ответа, S не подходит, дальше эту ветку не рисуйте. Если все первые ходы Пети разобраны и на каждый есть ответ, позиция годится.

Не переносите S из №20 в №21 без чтения вопроса. Иногда просят те же числа, иногда следующее по величине, иногда позицию, где выигрывает уже второй игрок.

## Шаблон на Python

Здесь рекурсия с глубиной хода. \`turns=4\` — четыре полухода, то есть второй ход Вани. Подстройте функцию под формулировку: кто выигрывает и не раньше какого хода.

${py(`
GOAL = 129
memo = {}

def moves(s):
    return [s + 1, s * 2]

def wins(s, turns_left):
    key = (s, turns_left)
    if key in memo:
        return memo[key]
    if s >= GOAL or turns_left == 0:
        memo[key] = False
        return False
    # текущий игрок выигрывает, если может сразу закончить
    # или оставить сопернику позицию, где тот не выигрывает
    result = False
    for m in moves(s):
        if m >= GOAL or not wins(m, turns_left - 1):
            result = True
            break
    memo[key] = result
    return result

# позиции, где первый не выигрывает за 1 и за 3 полухода,
# но второй выигрывает: проверяйте по точной формулировке варианта
for s in range(1, GOAL):
    if any(m >= GOAL for m in moves(s)):
        continue
    print(s, wins(s, 1), wins(s, 3))
`)}

Готовый \`print\` не копируйте в бланк. Сначала сопоставьте «победа за 3 полухода» с текстом: чей это ход и исключены ли более ранние победы. Шаблон считает факт победы не позднее заданной глубины. Для №21 почти всегда нужно ещё отсечь более быстрые победы.

## Типичные ошибки

- Дерево оборвано на ходе, где соперник ещё мог спастись.
- Победа за 2 хода второго игрока смешана с победой за 2 хода первого.
- Из №20 переписаны числа без проверки, что вопрос другой.
- В двух кучах ход применён к обеим сразу.
`,
  }),
  task({
    n: 22,
    label: "параллельные процессы",
    readingTime: 8,
    description:
      "Задание 22 ЕГЭ по информатике 2026: процессы, зависимости и время. Файл .ods, критический путь и ошибка «самая длинная цепочка».",
    keywords: ["процессы ЕГЭ", "многопроцессорность", "ods"],
    body: `
Задание 22 — повышенный уровень, 1 балл. Строят модель процессов: кто от кого зависит и сколько длится. Это про независимые процессы, которые могут идти параллельно, а не про очередь «строго по одному».

**Файл 2026 — \`.ods\`.** Колонки обычно такие: идентификатор, время выполнения, номера процессов, которые должны завершиться раньше. Пустая ячейка зависимостей значит, что процесс можно начать сразу.

## Конспект

Время старта процесса — 0, если предшественников нет, иначе максимум времён окончания предшественников. Окончание = старт + длительность.

Нужное в ответе бывает разным:

- момент окончания заданного процесса;
- сколько процессов выполняются в момент T, то есть старт ≤ T < окончание, либо с другими границами — смотрите «в момент» и «в течение»;
- минимальное время всей работы — максимум окончаний;
- сколько процессов не зависят друг от друга.

ФИПИ предупреждает: самая длинная цепочка по числу процессов не обязана быть самой длинной по времени. Ищите критический путь по сумме длительностей, а не по количеству звеньев.

Процесс не ждёт «всех, кто в таблице ниже». Он ждёт только перечисленных предшественников. Независимые процессы стартуют одновременно, если хватает процессоров. Если число процессоров ограничено, это уже другая модель: тогда нельзя запустить больше, чем есть процессоров, даже если зависимости позволяют. В большинстве вариантов процессоров хватает, ограничение — только зависимости. Прочитайте, есть ли лимит.

Время обычно целое. Если процесс длится 5 и стартовал в 0, в момент 5 он уже закончился, если интервал полуоткрытый. Условие «выполняется в момент t» надо сопоставить с тем, включается ли правый конец. На черновике нарисуйте отрезок для двух процессов и проверьте граничный момент, прежде чем писать формулу.

## Шаблон на Python

${py(`
# id -> (duration, list of prerequisite ids)
procs = {
    1: (4, []),
    2: (7, []),
    3: (3, [1, 2]),
    4: (2, [3]),
}

start = {}
finish = {}

def calc(i):
    if i in finish:
        return finish[i]
    dur, prev = procs[i]
    start[i] = max((calc(p) for p in prev), default=0)
    finish[i] = start[i] + dur
    return finish[i]

for i in procs:
    calc(i)

print(max(finish.values()))          # время всей работы
T = 5
running = [i for i in procs if start[i] <= T < finish[i]]
print(len(running), running)
print(finish[4])
`)}

Если идентификаторы в файле идут не по порядку и есть ссылка вперёд, рекурсия это переносит. Цикл зависимостей в условии не встречается. Если программа зациклилась, в данные попала ошибка чтения, не добавляйте \`setrecursionlimit\` вместо проверки файла.

## Типичные ошибки

- Время цепочки сложили как число процессов.
- Процесс стартует после суммы предшественников, а не после максимума.
- В момент T посчитан процесс, который в этот момент только что закончился.
- Пустую ячейку прочитали как ноль и приняли ноль за идентификатор процесса.
`,
  }),
  task({
    n: 23,
    label: "количество программ",
    readingTime: 8,
    description:
      "Задание 23 ЕГЭ по информатике 2026: сколько программ переводят число в число. Динамика по командам, обязательные и запрещённые точки.",
    keywords: ["количество программ", "траектория", "динамическое программирование"],
    body: `
Задание 23 — повышенный уровень, 1 балл. Исполнитель умеет несколько команд, например прибавить 1, прибавить 2, умножить на 2. Нужно число программ, которые переводят число A в число B. Иногда траектория обязана пройти через заданное число или не может его содержать.

ФИПИ не советует выписывать все пути. Их слишком много, и легко пропустить ветку. Рабочий способ — рекуррентная формула: сколькими способами можно получить текущее число из предшественников.

## Конспект

Пусть \`f(x)\` — число способов получить x из старта.

- \`f(start) = 1\`, одна пустая программа;
- \`f(x) = 0\`, если x недостижимо или запрещено;
- если в x можно прийти командой из y, то к \`f(x)\` прибавляют \`f(y)\`.

Для команд \`+1\` и \`*2\` удобнее идти от старта и раскидывать способы вперёд:

- из x команда \`+1\` добавляет \`f(x)\` к \`f(x+1)\`;
- команда \`*2\` добавляет \`f(x)\` к \`f(x*2)\`.

Запрещённое число обнуляют и не распространяют из него способы. Обязательная точка M: ответ равен \`f(M) * g(B)\`, где \`f\` — способы от старта до M, \`g\` — способы от M до финиша уже без повторного запрета на M. Нельзя просто посчитать пути от старта до B и вычесть пути мимо M, если не уверены в формуле. Произведение двух задач проще.

Если команды только увеличивают число, идти нужно в сторону роста и не заходить далеко за B. Команда вычитания требует другой порядок, от больших к меньшим, либо рекурсию с кэшем.

Команды «приписать цифру» превращают число в другое десятичное. Тогда предшественников ищут разбором, а не только \`x-1\`.

## Шаблон на Python

${py(`
start, goal = 1, 20
must = 8          # поставьте None, если обязательной точки нет
banned = {15}

def ways(a, b, forbid):
    f = [0] * (b + 1)
    if a <= b:
        f[a] = 1
    for x in range(a, b + 1):
        if x in forbid or f[x] == 0:
            f[x] = 0
            continue
        for nxt in (x + 1, x + 2, x * 2):
            if nxt <= b and nxt not in forbid:
                f[nxt] += f[x]
    return f[b]

if must is None:
    print(ways(start, goal, banned))
else:
    # команды только увеличивают число, поэтому в must повторно не заходят
    print(ways(start, must, banned) * ways(must, goal, banned))
`)}

Во второй задаче от M до цели саму точку M не запрещайте, иначе \`f(M)\` обнулится. Запрет на повторный заход в M нужен только если по условию через неё проходят ровно один раз и команды могут вернуться. При только увеличивающих командах вернуться нельзя, достаточно произведения.

В строке шаблона с \`banned | {must} if False\` запрет на M после старта не включается. Оставьте так для команд, которые не умеют вернуться. Если в варианте есть вычитание, запретите повторный вход явно.

## Типичные ошибки

- Пути через обязательную точку посчитаны как все пути до цели.
- Запрещённое число осталось транзитом: из него продолжили раскидывать способы.
- Команда \`*2\` применена к нечётному «делению», когда шли назад, и получили нецелые.
- Выписали полсотни траекторий и сбились на одной развилке.
`,
  }),
  task({
    n: 24,
    label: "обработка строки",
    readingTime: 9,
    description:
      "Задание 24 ЕГЭ по информатике 2026: длинная строка в файле .txt. Конечный автомат, один проход и шаблон на Python.",
    keywords: ["строки ЕГЭ", "конечный автомат", "файл 24"],
    body: `
Задание 24 — высокий уровень, 1 балл. Нужна программа примерно на 10–20 строк: обработать символьную строку из файла. Частичного балла нет.

**Файл 2026 — \`.txt\`.** Строка может быть длиной в сотни тысяч символов и больше. Квадратичный перебор подстрок не успеет. Нужен один проход.

ФИПИ описывает это как простейший конечный автомат с сумматором. Ошибка — не рассмотреть все пары «текущее состояние и встреченная буква».

## Конспект

Типовые вопросы:

- длина самой длинной цепочки из одинаковых символов;
- длина самой длинной подстроки, в которой не больше K вхождений символа;
- сколько раз встречается шаблон, возможно с перекрытием;
- цепочка, удовлетворяющая правилу чередования.

Автомат: вы идёте по строке и храните несколько чисел. Для цепочки одинаковых символов достаточно предыдущего символа и текущей длины. Для «не больше одной буквы A» — число A в текущем окне и левый край окна.

Два указателя, если окно сжимается слева. Левый двигается только вправо, каждый символ обрабатывается ограниченное число раз. Это по-прежнему один проход по смыслу линейного времени.

Перекрытие: в строке \`AAA\` подстрока \`AA\` встречается дважды, если перекрытия разрешены, и один раз, если ищут непересекающиеся. Условие это задаёт.

Файл может содержать перевод строки. Обычно это одна строка, но иногда несколько. Прочитайте, анализируют каждый ряд отдельно или весь текст. \`strip()\` не должен съесть значимые символы, если пробел входит в алфавит.

## Шаблон на Python

Самая длинная подстрока, в которой символ X встречается не больше K раз. Замените условие на своё.

${py(`
s = open("24.txt", encoding="utf-8").read().strip()
X = "A"
K = 2

left = 0
count_x = 0
best = 0
for right, ch in enumerate(s):
    if ch == X:
        count_x += 1
    while count_x > K and left <= right:
        if s[left] == X:
            count_x -= 1
        left += 1
    best = max(best, right - left + 1)
print(best)
`)}

Для подряд идущих одинаковых символов автомат короче: переменные \`prev\`, \`cur\`, \`best\`. Для шаблона фиксированной длины можно идти окном \`s[i:i+len]\`, если длина файла до миллиона, это ещё допустимо. Для правил с памятью не сравнивайте срез на каждом шаге, обновляйте состояние.

Проверьте шаблон на короткой строке, ответ которой посчитали руками, и только потом запускайте на файле. Файл A в этом задании обычно один.

## Типичные ошибки

- Не разобрана буква, которая сбрасывает автомат.
- Окно сдвинули на 1, хотя нужно было выкинуть символы, пока условие ложно.
- Перекрытия посчитаны или отброшены не по условию.
- Пробел и перевод строки вошли в цепочку.
`,
  }),
  task({
    n: 25,
    label: "делители и маски чисел",
    readingTime: 9,
    description:
      "Задание 25 ЕГЭ по информатике 2026: маски чисел и делители. Перебор, квадраты простых и шаблон на Python.",
    keywords: ["маски чисел", "делители", "задание 25"],
    body: `
Задание 25 — высокий уровень, 1 балл. Нужна программа примерно на 10–20 строк для перебора целых чисел: маска, делители, простые множители. В бланк вводят то, что программа напечатала, обычно пары чисел, по строке на каждое найденное. Код не сдают. Частичного балла нет: набор строк должен совпасть с эталоном, включая порядок.

Отдельного файла к линии 25, как правило, нет. Ограничения написаны в условии.

## Конспект

**Маска.** Символ \`*\` заменяет любую последовательность цифр, в том числе пустую, если не сказано иное. \`?\` обычно ровно одна цифра. Число не может иметь ведущий ноль, если оно не само ноль. Значит, маска \`*05\` не начинает перебор с пустой звёздочки, если получается \`05\` как запись с нулём впереди — уточните, считают ли такие числа. Чаще ведущие нули в найденном числе запрещены, а сама маска уже задаёт начало.

Перебирайте подставляемую часть циклом \`range\`, собирайте число и проверяйте делимость. Не генерируйте все числа от 1 до 10^12.

**Делители.** Чтобы найти все делители n, перебирайте d от 1 до \`sqrt(n)\`. Если \`n % d == 0\`, делители — d и \`n // d\`. Не забывайте, что при полном квадрате корень учитывают один раз.

**Ровно три делителя.** Такое натуральное число — квадрат простого: делители 1, p и p². Ровно два делителя — само простое.

**Поиск простых множителей.** Делите n на 2, затем на нечётные до корня. Если после цикла остаток больше 1, это последний простой множитель. ФИПИ просит проверить именно выделение множителей: типичная ошибка — посчитать делитель дважды или остановиться на первом.

Чётность, окончание на цифры, «нечётный делитель больше 1000» — фильтры после поиска делителей, не вместо него.

## Шаблон на Python

Маска \`123*567\`, звёздочка — любое число цифр, число делится на 123, в ответе число и целый частный. Ограничьте длину звёздочки так, чтобы покрыть диапазон из условия, и уберите ведущие нули, если они запрещены.

${py(`
def odd_divisors(n):
    divs = []
    d = 1
    while d * d <= n:
        if n % d == 0:
            if d % 2:
                divs.append(d)
            other = n // d
            if other != d and other % 2:
                divs.append(other)
        d += 1
    return sorted(divs)

def mask_numbers(max_digits=4):
    for length in range(0, max_digits + 1):
        limit = 10 ** length if length else 1
        for x in range(limit):
            mid = str(x).zfill(length) if length else ""
            s = "123" + mid + "567"
            if s[0] == "0":
                continue
            yield int(s)

for n in mask_numbers():
    if n % 123 == 0:
        print(n, n // 123)
`)}

Для «найти все числа от L до R с ровно пятью делителями» маска не нужна: пять делителей бывают у четвёртой степени простого. Перебирайте простые и возводите в четвёртую.

Проверьте на маленьком примере. Для 36 делители 1, 2, 3, 4, 6, 9, 12, 18, 36. Если скрипт их не собирает, до файла или маски его не пускайте. Здесь файла нет, но ошибка в \`sqrt\` так же портит ответ.

## Типичные ошибки

- Звёздочка не включает пустую подстановку, хотя по условию может.
- Корень-делитель продублирован.
- В ответ попал составной множитель вместо наибольшего простого.
- Числа напечатаны не в том порядке, в котором просит условие.
`,
  }),
  task({
    n: 26,
    label: "сортировка",
    readingTime: 9,
    description:
      "Задание 26 ЕГЭ по информатике 2026: целочисленные данные и сортировка. Файл .txt, два числа в ответе и когда ставят 1 балл из 2.",
    keywords: ["сортировка ЕГЭ", "задание 26", "два балла"],
    body: `
Задание 26 — высокий уровень, максимум 2 балла. Нужно обработать целые числа из файла: отсортировать и применить правило распределения, занятости или выбора. Можно писать свою сортировку, взять \`sorted\` или решить в электронной таблице. На практике файл длинный, программа короче и надёжнее.

**В 2026 году данные в \`.txt\`.**

Оценка по спецификации:

- 2 балла — оба числа верны и стоят в нужных ячейках;
- 1 балл — числа перепутаны местами или верно только одно;
- 0 — в остальных случаях.

Оба числа всё равно нужно получать из одной модели. Подгонять второе «на глаз» после первого не стоит: частичный балл не спасает, если модель неверна и случайно совпало только одно.

## Конспект

Прочитайте, что является ключом сортировки. Часто есть два поля: размер и время, или время начала и время конца. Сортируют не по тому полю, которое стоит первым в файле, а по тому, которое задаёт правило.

Направление важно. «Сначала самые короткие», «сначала более ранний конец», «при равенстве — меньший номер». При равенстве ключа второе поле обязательно зафиксируйте, иначе ответ скачет, хотя условие его определяет.

Типовые сюжеты:

- ряды полок или дисков: кладут файлы от больших к меньшим, пока помещаются, остаток переносят;
- события: сортировка по времени начала, счётчик одновременных, затем какое событие было последним в момент максимума;
- пары «приход, уход», отсортировать по концу и выбрать, сколько успевают обслужиться.

После сортировки не теряйте исходный номер, если он нужен в ответе. Храните кортеж \`(ключ, номер)\`, а не только ключ.

Первая строка файла часто содержит служебные числа: вместимость и количество записей. Не сортируйте её вместе с данными.

Проверка: на первых 10 строках повторите алгоритм руками. Если счётчик на маленьком куске совпал, гоняйте полный файл.

## Шаблон на Python

Сюжет «вместимость K, далее размеры, положить как можно больше, начиная с меньших, в ответ — сколько поместилось и максимальный размер оставшегося незанятого места, которое ещё можно было бы занять следующим, но уже не влезло» слишком привязан к формулировке. Ниже каркас: служебная строка, сортировка, жадное заполнение. Подставьте критерий отбора и то, какие два числа печатать.

${py(`
lines = [tuple(map(int, line.split())) for line in open("26.txt") if line.strip()]
header, items = lines[0], lines[1:]
capacity = header[0]
items = sorted(items, key=lambda row: (row[0], row[1]))

taken = 0
used = 0
last = None
for row in items:
    size = row[0]
    if used + size <= capacity:
        used += size
        taken += 1
        last = row
    else:
        break

print(taken, last[0] if last else 0)
`)}

\`break\` после первого непоместившегося верен только если дальше элементы ещё крупнее. Если сортировка по возрастанию размера, так и есть. Если порядок другой, непоместившийся сейчас может быть крупнее будущего подходящего — тогда \`continue\`, а не \`break\`. Это место сверьте с условием.

Второй ответ часто не «последний положенный», а максимальный элемент, который ещё можно поставить вместо кого-то или в остаток. Тогда после заполнения нужен отдельный поиск по неотложенным. Не печатайте \`last\`, пока не сопоставите его с вопросом.

## Типичные ошибки

- Неверное направление сортировки.
- Служебная строка попала в данные.
- При равенстве времени забыт номер, и выбран не тот объект.
- В ячейки ответа числа записаны в обратном порядке. Это ещё может дать 1 балл, но только если оба верны.
`,
  }),
  task({
    n: 27,
    label: "кластеры и анализ данных",
    readingTime: 11,
    description:
      "Задание 27 ЕГЭ по информатике 2026: анализ данных и кластеры точек. Центр кластера, файлы A и B, шаблон на Python и частичный балл.",
    keywords: ["кластеры", "анализ данных", "задание 27"],
    body: `
Задание 27 — высокий уровень, максимум 2 балла. По спецификации это последовательность шагов анализа данных: собрать данные, оценить качество, выбрать модель, преобразовать, при необходимости увидеть структуру, интерпретировать. В актуальных вариантах это кластеры точек на плоскости, часто «звёзды».

**Файл 2026 — \`.txt\`.** Обычно два файла: A — маленький, для отладки, B — большой, с тем же правилом и большим числом кластеров. Формат строки читайте в условии: иногда только x и y, иногда после координат есть классы светимости и лишние поля.

Оценка:

- 2 балла — обе строки ответа верны, пары чисел в правильном порядке;
- 1 балл — верна только одна пара, либо обе пары верны, но строки переставлены;
- 0 — иначе.

В бланк вводят числа, не программу.

## Что такое центр

Не подставляйте среднее арифметическое координат, пока не прочитаете определение. В нынешних формулировках центр кластера часто одна из точек этого кластера: та, у которой сумма расстояний до остальных точек кластера минимальна. Её обещают единственной.

Иногда после нахождения центров просят не координаты, а расстояние между центрами, число точек в полуплоскости, среднее центров, целую часть от координаты, умноженной на 10 000. Пишите ровно ту формулу, которая в условии. \`int(x * 10000)\` отбрасывает дробную часть и не округляет. Если x отрицательный, в Python \`int\` отбрасывает к нулю, а в условии может быть «целая часть» как округление к минус бесконечности. Для положительных координат это совпадает. Не округляйте через \`round\`.

Расстояние — обычное евклидово, если не сказано «по оси абсцисс». Расстояние по оси абсцисс — модуль разности x, без квадратного корня по двум координатам.

## Конспект

1. Разберите строку так, как она устроена. Два первых числа — координаты. Дальше могут идти пометки. Пропуск строки-мусора лучше сделать явно, чем поймать ошибку на середине файла B.
2. Файл A визуально кластеризуется даже без графика: напечатайте минимум и максимум x и y, посмотрите группы. На экзамене \`matplotlib\` может не быть, на него не опирайтесь.
3. Кластеры в этих задачах разделены пространством. Достаточно порога: две точки в одном кластере, если между ними расстояние меньше порога, и дальше по цепочке. Порог возьмите меньше зазора между облаками и больше диаметра облака. На файле A подберите его за минуту, глядя на разброс.
4. Число кластеров должно совпасть с условием. Если получилось больше или меньше, порог неверный, файл B не запускайте.
5. Для каждого кластера найдите центр по определению из условия. Для «точка с минимальной суммой расстояний» переберите точки кластера. Кластеры на файле B бывают по несколько тысяч точек, квадрат по кластеру допустим, квадрат по всему файлу — уже тяжело, поэтому сначала режьте на кластеры.
6. Аномалии. Если условие говорит отбросить выбросы или точки вне полос, сделайте это до центра. Иначе центр уедет.
7. Посчитайте две величины для файла A, сверьте с ручной оценкой, затем тот же код — на файле B. Не меняйте формулу между файлами, меняется только число кластеров и данные.

Альтернатива порогу: если кластеры явно разделены по x, отсортируйте по x и режьте в больших зазорах. Это быстрее, но ломается, если облака стоят друг над другом. Порог по полному расстоянию универсальнее.

Старый тип задания 27 — числовая последовательность и два указателя — всё ещё встречается в открытом банке. Если в условии не точки, а последовательность и «подпоследовательность с суммой», не применяйте кластеры. Сначала определите модель.

## Шаблон на Python

${py(`
def read_points(path):
    pts = []
    for line in open(path, encoding="utf-8"):
        parts = line.replace(",", ".").split()
        if len(parts) < 2:
            continue
        try:
            pts.append((float(parts[0]), float(parts[1])))
        except ValueError:
            continue
    return pts

def clusters(pts, eps):
    n = len(pts)
    used = [False] * n
    groups = []
    for i in range(n):
        if used[i]:
            continue
        stack = [i]
        used[i] = True
        group = []
        while stack:
            v = stack.pop()
            group.append(pts[v])
            x1, y1 = pts[v]
            for u in range(n):
                if used[u]:
                    continue
                x2, y2 = pts[u]
                if (x1 - x2) ** 2 + (y1 - y2) ** 2 <= eps * eps:
                    used[u] = True
                    stack.append(u)
        groups.append(group)
    return groups

def center(group):
    best = None
    best_sum = None
    for x1, y1 in group:
        s = 0.0
        for x2, y2 in group:
            s += ((x1 - x2) ** 2 + (y1 - y2) ** 2) ** 0.5
        if best_sum is None or s < best_sum:
            best_sum = s
            best = (x1, y1)
    return best

pts = read_points("27_A.txt")
groups = clusters(pts, eps=1.0)
print("кластеров:", len(groups), "размеры:", [len(g) for g in groups])
centers = [center(g) for g in groups]
print(centers)
# дальше — формула из условия, например целая часть среднего x * 10000
# px = sum(c[0] for c in centers) / len(centers)
# print(int(px * 10000))
`)}

Порог \`eps\` подберите на файле A. Если кластеров больше, чем обещано, увеличьте порог. Если один кластер поглотил два облака, уменьшите.

Когда центр определён как среднее, замените функцию \`center\`: среднее x и среднее y, это уже не точка из файла. Не смешивайте два определения в одной сдаче.

Производительность: поиск соседей в шаблоне квадратичный. Для файла A это незаметно. Для файла B при десятках тысяч точек может быть тесно по времени. Тогда группируйте сеткой: в список соседей попадают только точки близких ячеек. Пока файл до нескольких тысяч, прямой порог достаточен.

## Типичные ошибки

- Центр заменён на среднее, хотя нужна точка кластера с минимальной суммой расстояний.
- В кластер попали выбросы, центр сместился.
- Для файла B остался порог, который на A случайно дал 2 кластера, а на B склеивает области.
- В ответ попали координаты, хотя просили расстояние между центрами.
- Целая часть заменена округлением.
- Строки A и B переставлены. Это может оставить 1 балл, если обе пары сами по себе верны.
`,
  }),
];
